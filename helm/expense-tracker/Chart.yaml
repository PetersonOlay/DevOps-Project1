# Chart metadata for the expense-tracker Helm release (backend + frontend).
apiVersion: v2
name: expense-tracker
description: Expense & receipt tracker API (RDS Postgres + S3 receipts) on EKS
type: application
version: 0.1.0
appVersion: "1.0.0"
