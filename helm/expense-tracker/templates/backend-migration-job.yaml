{{- if .Values.migrationJob.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "expense-tracker.backend.name" . }}-migrate
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "expense-tracker.backend.labels" . | nindent 4 }}
  annotations:
    # Job specs are immutable, so a plain "helm upgrade" (image tag changes
    # every deploy) can't patch this in place. Running it as a post-install/
    # post-upgrade hook makes Helm delete the previous Job and create a fresh
    # one instead. Post- (not pre-) so the ServiceAccount/ConfigMap this Job
    # depends on are already installed by the time it runs.
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-delete-policy: before-hook-creation
spec:
  backoffLimit: 2
  template:
    metadata:
      labels:
        {{- include "expense-tracker.backend.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      restartPolicy: Never
      containers:
        - name: migrate
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
          command: ["node", "src/migrate.js"]
          envFrom:
            - configMapRef:
                name: {{ include "expense-tracker.backend.name" . }}-config
            - secretRef:
                name: {{ include "expense-tracker.backend.name" . }}-db-credentials
{{- end }}
